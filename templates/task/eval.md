<!--
eval.md 작성 메모
- 이 문서는 workflow 자체가 아니라 이번 task 수행 결과를 평가하는 용도다.
- `agent-drafted, human-confirmed`를 기본으로 한다.
- 각 축의 근거는 2~3줄 이내로 적는다.
- 문서 재요약을 길게 반복하지 않는다.
- 테스트 로그 원문을 복붙하지 않는다.
- `verification.md`, `review.md`의 핵심 근거만 압축해 사용한다.
- 근거 없는 낙관 판정은 피한다.
- 종합 판단 기준:
  - `Result=fail`이면 전체 판단은 `fail`
  - `Result=partial`이면 최소 `needs work`
  - `Result=pass`여도 `Process=missing` 또는 `Judgement=optimistic`이면 `needs work`
  - 세 축이 모두 양호하면 전체 판단은 `pass`
-->

# Eval

## 평가 대상
- task:
- 관련 문서:
  - `plan.md`
  - `verification.md`
  - `review.md`

## 에이전트 초안
### Result
- 제안:
- 근거:

### Process
- 제안:
- 근거:

### Judgement
- 제안:
- 근거:

## 사람 확인
- Result:
- Process:
- Judgement:

## 종합 판단
- 결론:
- 메모:
